# Larpaper's single user-editable configuration file.

# Start automatically after this many idle seconds.
IDLE_TIMEOUT=300

# Kitty window appearance.
FONT_SIZE=18
BACKGROUND='#000000'
BACKGROUND_OPACITY=1.0
MOUSE_HIDE_WAIT=0.01
CURSOR_SHAPE=block

# Terminal Text Effects settings.
FRAME_RATE=120
CANVAS_WIDTH=0
CANVAS_HEIGHT=0
ANCHOR_CANVAS=c
ANCHOR_TEXT=c
EFFECT_PAUSE=1
TTE_EFFECT_ARGS=(--random-effect)
